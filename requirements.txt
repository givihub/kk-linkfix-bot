# DevSecOps: версии пинованы
aiogram==3.30.0
aiohttp-socks==0.11.0  # нужен aiogram для работы через прокси (PROXY_URL)
yt-dlp==2026.8.19      # запасной добытчик видео напрямую с платформ (мимо фиксеров); YouTube требует свежих версий
tzdata==2026.3         # часовые пояса для режима тишины (в slim-образе их нет)
